#!/bin/bash
DIR=$(readlink -f "$(dirname "$0")")
PROJECT_ROOT=$DIR

cd "$PROJECT_ROOT" || exit 1
git switch installation

for d in $(ls -d */); do
  dir=$(echo "$d" | head -c -2)
  stow -t "$HOME" -D "$dir"
done
